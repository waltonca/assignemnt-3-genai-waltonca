install Python 3.11.x

# Create virtual Env, For MacOS
python3 -m venv .venv

# Activate virtual env, For MacOS
. .venv/bin/activate //.space.

# Install dependencies
pip install flask
pip install torch
pip install transformers

# to run the flask app
flask --app app run